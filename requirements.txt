# ---- Torch (CUDA 12.6) ----
# pip cannot scope an index to a single package, so torch must be
# installed separately to guarantee the cu126 build:
#
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
#
# Then install everything else:
#
#   pip install -r requirements.txt
#
# Or use the one-liner:
#
#   pip install torch --index-url https://download.pytorch.org/whl/cu126 && pip install -r requirements.txt
#
# Do NOT add torch to this file — it will resolve against PyPI and may
# pull a CUDA 13.0 build incompatible with driver 565.77 (CUDA ≤12.7).

# Core
tiktoken
rustbpe

# Data loading (ClimbMix shards)
pyarrow
requests

# Evaluation (GPT2-large scoring, optional)
transformers

# Testing
pytest
